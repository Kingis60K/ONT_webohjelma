{
  "name": "ont-webohjelma-insecure-shop",
  "version": "0.1.0",
  "private": true,
  "description": "Deliberately vulnerable training app demonstrating SQL injection risks (closed environment only).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mysql2": "^3.11.0"
  }
}
